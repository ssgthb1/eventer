# Same Supabase project as apps/web (RLS enforces access on the anon key).
# These vars are inlined at build time by Expo — the EXPO_PUBLIC_ prefix is required.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
